#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int number, sum = 0;
    while (1) { 
        scanf("%d",&number);
        if (number == -1) {
            break;
        }
        sum += number;
    }
    printf("%d\n",sum);   
    return 0;
}
